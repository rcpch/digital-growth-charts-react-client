#!/usr/bin/env bash
#
# Audit the committed npm lockfile and fail on high-severity advisories.

set -euo pipefail
cd "$(git rev-parse --show-toplevel)"

exec npm audit --package-lock-only --audit-level=high "$@"
