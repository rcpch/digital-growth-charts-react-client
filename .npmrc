# semantic-ui-react (3.0.0-beta.2) declares React peer ranges capped at ^18,
# but the 3.0.0-beta line removed the Ref/findDOMNode and defaultProps usage
# that React 19 broke. Install with relaxed peer resolution until it declares
# React 19 support.
legacy-peer-deps=true